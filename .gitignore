# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Environment & editors
.env
.idea/
.vscode/
.DS_Store
*.log
!samples/*.log

# Runtime data (SQLite DB, uploads) — lives in data/ locally or in the Docker volume
data/

# Offline GeoIP database (~120 MB) — fetch with: python scripts/download_geoip.py
geoip/*.mmdb

# Local-only material (old UI captures, scratch work)
ui-ux-screenshots/
scratch/
node_modules/
